export const environment = {
    // @if NODE_ENV = 'development'
    env: 'https://retail-mall-develop.51zan.com',
    // @endif
    // @if NODE_ENV = 'test'
    env: 'https://retail-mall-test.51zan.com',
    // @endif
    // @if NODE_ENV = 'production'
    env: 'https://retail-mall.51zan.com',
    // @endif
};